# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Audio Conversion Configuration
# Sample rate for AMR to WAV conversion (8000 for 8kHz phone calls, 16000 for higher quality)
# Default: 16000 (16kHz) - most APIs work better with 16kHz
AUDIO_SAMPLE_RATE=16000

# Gemini Configuration
# Gemini model to use for transcription (gemini-2.0-flash-lite, gemini-1.5-flash, etc.)
# Default: gemini-2.0-flash-lite
GEMINI_MODEL=gemini-2.0-flash-lite

# Optional: Default folder (can be overridden via command line)
# DEFAULT_FOLDER=./input
